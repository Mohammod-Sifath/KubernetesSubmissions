apiVersion: v1
kind: Service
metadata:
  name: log-output-svc
  namespace: exercises
spec:
  type: NodePort
  selector:
    app: log-output
  ports:
    - port: 80        # service port (what port-forward / Ingress uses)
      targetPort: 3000  # pod container port

